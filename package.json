{
	"private": true,
	"scripts": {
		"bootstrap": "lerna bootstrap --hoist",
    "test": "lerna run test",
    "prepare": "lerna run prepare"
	},
	"devDependencies": {
		"lerna": "^2.9.1",
		"tslint": "5.9.1",
		"tslint-config-prettier": "^1.10.0"
	}
}
